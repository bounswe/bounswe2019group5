// this component is copied from https://github.com/ydeshayes/react-highlight
// and some of its part is changed for annotation because it doesn't provides
// something we need.

import Highlightable from './Highlightable';

export default Highlightable;
export const Range = require('./Range');
